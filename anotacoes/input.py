# O input garante que uma variável tenha um valor dado pelo usuário
# sua sintaxe é:

nome = input('Qual seu nome? ')

# Neste caso, o input está pegando valores somente de strings, e o conteúdo dentro do parenteses
# vai ser mostrado ao usuário no terminal, geralmente como uma pergunta ou ordem.

numero = int(input('Digite um número '))

# Para pegar um valor numérico, basta colocar o 'int()' antes do input.
# Isso vale para todas as variáveis!

n1 = int(input('Digite um valor '))
print(type(n1))

# Essa sintaxe de 'type' mostra o valor da variável.

